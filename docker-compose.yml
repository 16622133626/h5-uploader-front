version: '3.8'

services:
  app:
    build: .
    ports:
      - "3001:3001"
    environment:
      - PORT=3001
      - UPLOAD_DIR=./uploads
    volumes:
      - ./server/uploads:/app/server/uploads
      - ./server/data:/app/server/data
    restart: unless-stopped






